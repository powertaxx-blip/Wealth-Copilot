import { Trusts } from "@/components/features/Trusts";
import type { Metadata } from "next";

export const metadata: Metadata = { title: "Trusts" };

export default function TrustsPage() {
  return <Trusts />;
}
