import { ScheduleC } from "@/components/features/ScheduleC";
import type { Metadata } from "next";

export const metadata: Metadata = { title: "Schedule C" };

export default function ScheduleCPage() {
  return <ScheduleC />;
}
