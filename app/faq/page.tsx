import { ComingNext } from "@/components/ui/ComingNext";

export default function FaqPage() {
  return (
    <ComingNext
      title="FAQ & Ask a Question"
      description="A curated, searchable library of real client questions — the first candidate to become a real LLM-grounded assistant per the system blueprint's Phase 1."
      oldPanelId="faq"
    />
  );
}
