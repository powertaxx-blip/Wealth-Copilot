import { ComingNext } from "@/components/ui/ComingNext";

export default function EmergencyPage() {
  return (
    <ComingNext
      title="Emergency Fund Monitor"
      description="Tracks months of expense coverage against a target and a save-by date."
      oldPanelId="emergency"
    />
  );
}
